---
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/Dashboard/EmotionChart.tsx
autonomous: true
---

# Plan 02-01: Recharts Emotion Breakdown & Contextual Tooltips

## Objective
Replace the monolithic HTML inline bar chart in `App.tsx` dealing with "Emotion Breakdown" into an encapsulated and beautifully animated Recharts Component supporting Contextual Text tooltips.

## Requirements Addressed
- VIS-02: Add interactive tooltip and hover effects on the emotion breakdown chart
- VIS-03: Add CSS/SVG animations

## Tasks

<task>
<read_first>
- frontend/src/App.tsx
- frontend/package.json
</read_first>
<action>
Create a new file `frontend/src/components/Dashboard/EmotionChart.tsx`.
1. Code a React functional component exporting `EmotionChart`. It accepts `distribution` (Record of emotion to numbers) and `rawResults` (`CommentResult[]`) as props.
2. Inside, use Recharts `<BarChart>` feeding it a formatted `{name, value}` array.
3. Build a custom `CustomTooltip` function satisfying the Recharts tooltip signature. When hovering over an emotion (e.g., "Joy"), grab a sample `text` from `rawResults` where `emotion === "Joy"` and render it as the contextual quote.
4. Apply standard Recharts animations: Set `isAnimationActive={true}` on the `<Bar>`. Use a vivid palette array and map the `<Cell fill={color}>` for distinct emotions.
5. In `frontend/src/App.tsx`, replace the `<div className="simple-bar-chart">` under the **Emotion Breakdown** header with `<EmotionChart distribution={data.summary.emotion_distribution} rawResults={data.results} />`.
</action>
<acceptance_criteria>
- File `frontend/src/components/Dashboard/EmotionChart.tsx` is created and exports `EmotionChart`.
- `EmotionChart.tsx` imports `{ BarChart, Bar, Cell, Tooltip, ResponsiveContainer }` from `recharts`.
- `EmotionChart.tsx` contains a custom tooltip parsing contextual text quotes from `rawResults`.
- `App.tsx` imports and renders `<EmotionChart ... />`.
- `<div className="simple-bar-chart">` for Emotion Breakdown is purged from `App.tsx`.
</acceptance_criteria>
</task>

## Verification
- Validate compilation executing `cd frontend && npm run build`.
