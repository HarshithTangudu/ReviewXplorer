---
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/Dashboard/SentimentCharts.tsx
autonomous: true
---

# Plan 02-02: Gauge & Animated Timeline for Sentiment

## Objective
Implement the Sentiment Gauge Chart (Top-level) and the Sentiment Timeline (Scraped Sequence) visualizations to meet the complex data-viz contextual demand.

## Requirements Addressed
- VIS-01: Implement dynamic sentiment visualization (gauge chart or animated timeline)
- VIS-03: Add CSS/SVG animations

## Tasks

<task>
<read_first>
- frontend/src/App.tsx
</read_first>
<action>
Create a new file `frontend/src/components/Dashboard/SentimentCharts.tsx`.
1. Code two specialized components: `SentimentGauge` and `SentimentTimeline`.
2. **`SentimentGauge`**: Implement a Gauge by leveraging a Recharts `<PieChart>` rendered as a semi-circle (`startAngle={180}` `endAngle={0}`). Pass a prop for total aggregated sentiment score.
3. **`SentimentTimeline`**: Pass `data.results` to this. Use an `<AreaChart>` mapping X-axis to the review's sequence index, and Y-axis to sentiment strength (e.g Positive = 1, Neutral = 0, Negative = -1). Apply `isAnimationActive={true}`.
4. Add sleek CSS `.chart-enter-active` classes if needed for mount transitions.
5. In `App.tsx`, delete the current "Sentiment Distribution" hardcoded div list. Insert `<SentimentGauge />` and `<SentimentTimeline />` appropriately inside the primary dashboard layout.
</action>
<acceptance_criteria>
- `frontend/src/components/Dashboard/SentimentCharts.tsx` imports `{ PieChart, AreaChart }` from `recharts`.
- `SentimentGauge` utilizes standard Recharts pie geometries limited to 180 degrees.
- `App.tsx` relies natively on `<SentimentGauge>` and `<SentimentTimeline>`.
- The original HTML template `<div className="simple-bar-chart">...{sentimentData.map...</div>` is removed from `App.tsx`.
</acceptance_criteria>
</task>

## Verification
- Validate syntactical compilation with `npm run lint`.
